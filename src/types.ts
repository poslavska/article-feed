export type ArticleType = {
  id: number,
  image_url: string,
  published_at: string,
  summary: string,
  title: string
}

export type ArticleShort = {
  id: number,
  image_url: string,
  summary: string,
  title: string
}